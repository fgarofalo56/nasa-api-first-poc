# client — Python CLI
`query_supply_risk.py --program Artemis-3 --min-delay 30`: bearer flow -> calls Kong ->
prints the ranked answer + the gateway correlation id (proves it went through the gateway).
Build per PRP §6/§8 Phase 5.
