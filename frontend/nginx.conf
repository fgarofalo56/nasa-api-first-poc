server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # config.js is runtime config — never cache it.
    location = /config.js {
        add_header Cache-Control "no-store";
    }

    # SPA fallback.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
